#2.配置相关信息
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ssm_db?serverTimezone=UTC
    username: root
    password: root

#设置Mp相关的配置
mybatis-plus:
  global-config:
    db-config:
      #设置所有表的通用前缀名称为tbl_
      table-prefix: tbl_
